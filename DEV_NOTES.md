# Dev notes
Install (just once)
- `pip install -e .`

Run the project
- `void`
- `python -m void.main`
- `python void/main.py`

Debugging
1. Terminal 1 `textual console`
2. Terminal 2 `textual run --dev  void/main.py`
